---
title: "STIGMERGY v2.0 — Scheduler watch-and-steer baton"
born: 2026-06-22
links:
  - target: "[[STIGMERGY v2.0 — Consolidation & Primary Interface]]"
    type: connects-to
    label: "baton-for"
forward_vector: "I carry the next STIGMERGY v2.0 move — the autonomous scheduler's watch-and-steer surface — across a deliberate pause, to be caught by the next Claude and deleted once the move is picked up."
---

# Baton: STIGMERGY v2.0 — scheduler watch-and-steer surface

## Move
Build the STIGMERGY surface that makes the autonomous steward scheduler **watchable
and steerable from the terminal**. Scope is locked (Loudon's call this session):
**watch + a global pause/resume**. Not per-steward cadence/control (that's a
deferred follow-on).

## Why this move matters
The roadmap calls the scheduler "the watch-and-steer counterpart to everything
above," and the v2.0 thesis is *STIGMERGY as the primary surface*. The planning
session this round found the scheduler is **not greenfield** — the auto-fire
mechanism already exists as `_ops/heartbeat/` launchd machinery (`run-steward-batch.sh`
+ plists + the orchestrator's `batch.md`/`batch-plan.js`). So the real value isn't
building a scheduler; it's making the existing one **legible and steerable** instead
of an invisible cron. Sharpening that: right now it's a *black box that isn't even
running* — surfacing it truthfully (including a loud "not installed") is the point.

## Tried and rejected (this session — don't re-explore)
- **Treating the scheduler as greenfield** → no. It's mostly built; don't rebuild it. Surface it.
- **Following `Orchestrator Production Plan v0.2.md` literally** → no, it's STALE (2026-05-04). Its core decision ("schedule via Cowork's `schedule` skill, documented recipes only, no custom infra") was **reversed**: launchd Mac-side replaced Cowork because the sandbox lacked GPU/Max. Reconcile that doc to reality as part of this move; don't execute its phases.
- **Option A (per-steward cadence.js + spawn-from-project + retire/pause/resume)** → deferred. Loudon picked watch-and-steer (B) over A. Per-steward control is a follow-on (it overlaps the registry-status work).
- **STIGMERGY shelling out to `launchctl` to pause/resume** → rejected (shell/permission risk). Use a **`.paused` flag-file** the wrapper checks and no-ops on; STIGMERGY only toggles the flag.
- **A new sixth deck** → no. Extend the **STEWARDS deck** (the roster's natural home); keep the five-deck identity.

## Current state (closing-well punchlist)
- **3 increments LANDED on `main` this session** (all FF-landed via the owner, worktrees torn down, suites green at each): `ed1ebdd` vector-tuning surfaced in the terminal · `e93e2c1` stage-transition audit + `set-stage` op · `6852b2f` label-enrichment audit + `set-label` op. **Owner `main` = `6852b2f`** at handoff (it's a multi-agent repo — re-check + rebase/FF as usual).
- The Weave **posting half** now exposes **5 QUEUE audit triggers** (unsung-path · hub-promotion · stage-transition = mechanical detection; vector-tuning · label-enrichment = generative) and the **executor-op set is CLOSED**: `set-vector` / `add-link` / `set-type` / `set-stage` / `set-label`. Full app **1447** + orchestrator **227** vitest at `6852b2f`.
- **Decisions made (do NOT re-litigate):** (1) direction = **watch-and-steer in STIGMERGY**; (2) MVP scope = **watch + global pause/resume** (per-steward control deferred).
- **Scheduler ground truth, verified this session** — the surface must show these honestly:
  - `launchctl list` → **no palace jobs loaded**.
  - **no `_ops/heartbeat/.last-steward-batch` stamp** → never run / rolled back.
  - `trickster-auto/heartbeat-latest.md` digest is from **Jun 8** (stale).
  - `_ops/stigmergy/orchestrator/src/registry.js` has **no** retire/pause/resume/status.
  - The heartbeat machinery exists in-repo but is **NOT installed/firing**.
- **Not done — the next Claude's whole job:** no scheduler-surface code written yet. This round was planning + the 3 unrelated Weave increments above.

## Next move
Stand up a worktree and build, per the design agreed this session:
- **Home:** a "schedule" strip on the **STEWARDS deck** (extend, don't add a deck).
- **Watch (read):** a `GET /api/scheduler/status` that reports — is the launchd job loaded · its cadence (from the plist) · last run (stamp + newest `_ops/heartbeat/logs/*.log`) · next expected fire · a loud ⚠ when **not installed** (today's state). Augment the existing steward rows (`steward-lane.js` `stewardRow`) with `last_active` / iteration / `grants_waiting` / "due next run?" (reuse `batch-plan.js`'s debounce). Render `trickster-auto/heartbeat-latest.md` inline, **with its age shown**.
- **Steer (write):** global pause/resume via a **`_ops/heartbeat/.paused` flag-file** — add a one-line guard near the top of `run-steward-batch.sh` (no-op + log if present), and a `POST /api/scheduler/pause` + a toggle button. STIGMERGY never runs `launchctl`. "Run batch now" already exists (STEWARDS `advanceAll`) — don't rebuild it.
- **First milestone:** make the status *truthful* (shows "not installed" until launchd is actually loaded) — that's the honest floor before anything else.
- **Also:** reconcile `Orchestrator Production Plan v0.2.md` to the launchd reality (it's a misleading build contract right now).
- Tests + a dry e2e as usual; **do not** trigger a real batch / install launchd jobs from the build (that's Loudon's operational setup).

## Calibrations from this session
- **Worktree-first** for the build: `node _ops/worktree/new-worktree.mjs --name feature/stigmergy-scheduler-ui --profile stigmergy --memory`. FF-land via owner (`git -C "<owner>" merge --ff-only <branch>`), verify owner stays on `main`, tear down + clear the dangling `~/.claude/.../memory` symlink.
- **The e2e port-5173 gotcha (cost a false failure this session):** Playwright's `reuseExistingServer` reuses ANY listener already on :5173 — including a stale dev server from a prior turn without your new code → false e2e failures. **Kill any stale 5173 listener before e2e** (`lsof -tiTCP:5173 -sTCP:LISTEN | xargs kill`), and don't leave dev servers running between turns.
- The scheduler surface is **NOT a Weave audit** — don't pattern-match it onto the QUEUE audit-trigger shape. It's the STEWARDS deck + a new `/api/scheduler/*` namespace + a wrapper-script change. New ground.
- Stage explicit pathspecs on the shared owner tree — **never `git add -A`**; verify branch before+after every commit. Commit `ops(stigmergy): …` + `Palace-Kind: ops` / `Palace-Verify: verified` / `Palace-Author: claude`; subjects ≤72 chars.
- The roadmap-doc edit folds into the build commit (as every increment this session did).

## Load these files first
1. `Palace development/STIGMERGY v2.0 — Consolidation & Primary Interface.md` — the roadmap; the three `2026-06-22` bullets are what shipped, "Still open" is the frontier (scheduler + mirror sweeps).
2. `Palace development/Orchestrator Production Plan v0.2.md` — the **stale** written scheduler contract; reconcile, do not execute literally.
3. `_ops/heartbeat/README.md` + `run-steward-batch.sh` + `launchd/*.plist` — the real autonomy mechanism to surface + add the `.paused` guard to.
4. `_ops/stigmergy/app/server/steward-lane.js` — the STEWARDS deck's existing `list`/`advance`/`advanceAll` + `stewardRow` (the per-steward data the watch surface extends) + the STEWARDS deck component under `_ops/stigmergy/app/src/components/`.
5. `_ops/stigmergy/orchestrator/src/batch-plan.js` (+ `registry.js`, `process-cycle.js`) — the due-steward planner = the "due next run?" data source.
6. `_ops/stigmergy/trickster-auto/heartbeat-latest.md` — the digest to render with age.
7. `git log --oneline --grep='ops(stigmergy)' -14` on `main` — this session's build journal (`ed1ebdd` → `6852b2f`) + the prior posting-half arc.

## On pickup (fixed — the catcher's checklist; do not rewrite per session)
1. State the move back in one sentence. If you can't, the baton wasn't caught — stop and ask Loudon.
2. If this baton or its board line is still uncommitted (authored on a surface that couldn't commit — e.g. Cowork), commit them first. That commit is the git archive Step 6 relies on.
3. Mark it caught: remove the "Active Baton" section from the parent entry; for a board-announced baton with no parent entry, post the paired `handoff_picked_up` REPLY (`re:` the `handoff_ready` id) instead.
4. Delete the baton file (git is its archive). On a surface that can't delete (Cowork), remove the marker and note "deletion pending."
5. If the baton names a receiving-surface capability delta or a worktree coordinate, confirm it holds before relying on it (the [[Surfaces and Capabilities]] catalog can be stale) — for a worktree, check `git worktree list` and recreate it (`node _ops/worktree/new-worktree.mjs --name <branch> --profile <p>`) if it is gone. A build that was supposed to run here but can't is a finding to report, not a failure to hide.
6. Act on the move, holding the calibrations above. Steward batons are the exception — updated in place, never deleted.
