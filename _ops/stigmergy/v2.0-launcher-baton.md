---
title: "STIGMERGY launcher — baton"
born: 2026-06-17
links:
  - target: "[[STIGMERGY v2.0 — Consolidation & Primary Interface]]"
    type: connects-to
    label: "baton-for"
forward_vector: "I carry the next move on the STIGMERGY interactive launcher across a session boundary, to be caught by the next Claude and deleted once the move is picked up."
---

# Baton: STIGMERGY interactive launcher

## Move
Teach stewards to **self-emit a "request interactive" mark** at a critical moment. The launcher already READS + WAKES a steward as a page-agent (construct panel), and a TRICKSTER card routes a registered steward to it — but a steward can't yet ASK for a session itself. Close that loop.

## Why this move matters
It's the one named-but-unbuilt half of the roadmap's "Launch Interactive → stewards + steward-requested sessions." The whole UI/server side is built + proven; the gap is purely the steward *emitting* the signal. Small, bounded, completes the arc — unlike the two larger alternatives (Orchestrator v0.2 / the autonomous scheduler; wiring Weave proposals end-to-end), which are new directions.

## Tried and rejected (this session — don't re-explore)
- `--model opus` alias → resolved to Opus **4.7-low-effort** on Claude Code v2.1.x. Pinned exact id `claude-opus-4-8 --effort high`.
- Desktop deep-link + `node-pty` in-browser terminal → both **verified unsupported** (no URL scheme; TTY/keychain). A real Terminal via osascript is the path.
- Hand-rolled prose steward prompt → replaced by `buildCyclePrompt(mode:'interactive')` (Pages-as-Agents; one source of truth). Don't reinvent it.
- Construction panel on QUEUE handoff/enrichment cards → rejected: those are *tasks*, not page-agents; they keep the simple LaunchModal + LAUNCH TERMINAL.

## Current state
**Everything landed on `main`, verified, server live on :5173.** Nothing half-written. The seam for the move: a steward posts a `RESOURCE_REQUEST` with `payload.kind:'interactive_session'` (reuse the type — no Schema Ceremony) → the TricksterCard detects the kind and surfaces the construct-agent launch (the card already routes registered stewards to `AgentLaunchModal`). The harder half is the EMIT side — an orchestrator cycle-prompt change in `steward.md` teaching *when* to ask. That's a live-behavior change: go carefully, show Loudon.

## Next move
Wire the read/surface side first (cheap, in the UI you already built), then the careful `steward.md` emit side. Build in a worktree; verify; land to main; **restart the server** (server-side changes don't HMR).

## Calibrations from this session
- **Worktree-first:** `node _ops/worktree/new-worktree.mjs --name feature/<x> --profile stigmergy --memory` (auto-mirrors node_modules; teardown sweeps + safe `-d` delete).
- **Branch-verify before AND after every commit** (shared tree thrashes); explicit pathspecs, never `git add -A`; FF or `--no-ff` to land (main may have advanced — blueline).
- **Server-side changes need a dev-server restart**; client-only HMRs.
- **Tests:** `npx vitest run` in `app` (~1256) + separately in `orchestrator` (~227). Touched-surface Playwright only — the live :5173 fires REAL `claude -p` cycles; never run stewards.spec's fire path.
- **LAUNCH TERMINAL is unproven end-to-end** — first real click triggers a one-time macOS "control Terminal" grant only Loudon can approve.
- **Never write the blackboard via shell** — the classifier blocks it (rightly).
- Loudon's pace: tight increments, verify in-browser + screenshot, land each before the next.

## Load these files first
1. `Palace development/STIGMERGY v2.0 — Consolidation & Primary Interface.md` — roadmap + the three threads (the menu).
2. `git log --oneline -12` on main — the `ops(stigmergy)` build journal.
3. `_ops/stigmergy/orchestrator/src/build-cycle-prompt.js` — the `mode`/`include` options.
4. `_ops/stigmergy/app/server/api/launch.js` + `src/components/queue/AgentLaunchModal.jsx` — route + panel.
5. `_ops/stigmergy/app/src/components/TricksterDeck.jsx` — the steward-routing (read-side of "request interactive").
6. `.claude/skills/palace-orchestrator/prompts/steward.md` — where the EMIT side gets taught.

## On pickup (fixed — the catcher's checklist; do not rewrite per session)
1. State the move back in one sentence. If you can't, the baton wasn't caught — stop and ask Loudon.
2. This is a board-less, same-surface baton — mark it caught by deleting this file as your first act (git is its archive).
3. Build the move per "Next move," holding the calibrations above.
