# Pheromone traces (Arm C seed)

`worker_trace` blocks simulating a prior weave cycle's accumulated signal. In Arm C
each worker reads ONLY its own trace before auditing (not peers' live flags). Derived
from control-arm convergence, reframed as persistent cross-cycle signal ("look here first").

## Hilaritas neighborhood

**Hilaritas Generator**
```yaml
worker_trace:
  last_weave: prior-cycle
  finding: "Multiple prior workers converged on an unformalized Threshold Conatus hub forming in this neighborhood."
  status: unresolved
  signal_strength: high
  directive: "Examine whether this entry should link to Threshold Conatus before anything else."
```

**Spinoza Conatus**
```yaml
worker_trace:
  finding: "Reciprocal links to Tristitia Generator and Endosymbiosis proposed in prior cycles but never applied; confidence-field value flagged non-canonical 2 cycles running."
  status: unresolved
  signal_strength: high
```

**Cooperation Yields Agency**
```yaml
worker_trace:
  finding: "Threshold Conatus link repeatedly proposed; confidence value non-canonical."
  status: unresolved
  signal_strength: medium
```

**Striatum**
```yaml
worker_trace:
  finding: "Spinoza Conatus and Tristitia Generator bridges proposed across prior cycles; the dopamine/reward-prediction-error substrate keeps surfacing as a Tristitia coupling."
  status: unresolved
  signal_strength: high
```

**Tristitia Generator**
```yaml
worker_trace:
  finding: "Striatum bidirectional bridge is a recurring high-signal proposal; entry has never been revived (activation_count 1) despite generative open questions."
  status: unresolved
  signal_strength: high
  directive: "Look first at the Striatum coupling and at your own revival."
```

## Kuramoto neighborhood

**Kuramoto Coupling**
```yaml
worker_trace:
  finding: "Flocking sibling link flagged high and unresolved across cycles; confidence value non-canonical."
  status: unresolved
  signal_strength: high
```

**Hyperdimensional Prism**
```yaml
worker_trace:
  finding: "Action Potential Oscillator links to Prism but Prism never links back; structurally significant body wikilinks remain unregistered."
  status: unresolved
  signal_strength: high
  directive: "Resolve the APO reciprocal link first."
```

**Action Potential Oscillator**
```yaml
worker_trace:
  finding: "Granular Synthesis concept-hub gap and the scientific-sonification-claim link remain unresolved across cycles."
  status: unresolved
  signal_strength: high
```

**Lateral Access**
```yaml
worker_trace:
  finding: "Forward vector flagged weak for rewrite; Oblique Portrait body-prose links unresolved; filter taxonomy has no carrier entry."
  status: unresolved
  signal_strength: medium
```

**Mixture of Experts**
```yaml
worker_trace:
  finding: "Hub promotion overdue (11 typed links); a Lateral Access mirror was independently proposed in a prior cycle."
  status: unresolved
  signal_strength: high
```
