---
title: Deposit Archive
type: meta
pillars:
  - practice
born: 2026-03
stage: foundational
status: canonical
links:
  - target: "[[Deposit Ceremony]]"
    type: emerged-from
  - target: "[[Harvest Ceremony]]"
    type: connects-to
    label: archive-of
---

# Deposit Archive

**Frozen 2026-06-19 — pre-spec record only.** A completed deposit's record is now *its commit*: STIGMERGY renders the Deposit Archive as the LOG deck filtered to `Palace-Kind: deposit`, reading each synthesis from the commit body. There is no row to append — the deposit ceremony writes the summary into the commit (see [[Deposit Ceremony]] § Step 7b).

The table below is preserved unedited as the historical archive of every deposit committed *before* the migration. These commits predate the `deposit(<scope>):` subject spec and do not all self-classify on the LOG deck, so their hand-written summaries live here and nowhere else. **Do not append to it.**

| ID | Source | Date | Topic | Status | Summary |
|---|---|---|---|---|---|
| SPONT | cowork-session | 2026-03-24 | Signal-rate CV architecture + Action Potential Oscillator enrichment | worthy | | done | New entry: Signal-Rate CV Architecture (seed). Enriched: Action Potential Oscillator (Gen~ implementation section, citation corrections, resolved Faust/Gen~ question, artifact reference). Artifact: neural_oscillator_dev_plan.md |

| SPONT2 | cowork-session | 2026-03-24 | State machines, progressive staging, neural oscillator implementation | worthy | | done | Spontaneous deposit. New entries: [[State Machine]] (concept, sprout), [[Progressive Staging]] (practice, seed). Updated: [[Action Potential Oscillator]] (implementation status, new links to State Machine and Progressive Staging). Key threads: state machine best practices across Gen~/codebox/JS/Python/Faust, visual-to-textual threshold at 4 states, staged pedagogy where every stage is playable, V_start_actual History pattern, latch pattern for waveform continuity. |
| SPONT3 | claude.ai BBS/Map session | 2026-03-26 | Palace Map, BBS Blackboard, Trickster swarm role, Map Build Ceremony | worthy | | done | Spontaneous deposit. New entries: [[Palace Map]] (concept, sprout), [[BBS Blackboard]] (concept, sprout), [[Map Build Ceremony]] (meta, seed). Updated: [[Trickster]] (new section on swarm role + links), [[Swarm Weave]] (new links to BBS Blackboard and Palace Map), [[Palace Ceremonies]] (Map Build registered). Key threads: full/bounded map survey, ghost nodes as forward tension, BBS stigmergy model, append-only JSON blackboard, board routing, agent IDs from manifest, TRICKSTER as interruptor/catalyst. |
| SC01 | claude.ai Schema/RDP session | 2026-03-28 | Schema Ceremony v1.5 — optional label field on link objects | worthy | | done | Schema Ceremony. CLAUDE.md → v1.5. Updated: SCHEMA.md (Section 3 link object fields, Section 4 label paragraph), ROSETTA.md (Section 4 label note), CLAUDE.md (version bump + Key Vocabulary note), Weave Ceremony.md (Step 3a label note + new Step 3c label enrichment), Deposit Ceremony.md (Pre-step Intent Declaration, Tension Map, label prompt in Typed links, Latent Variable Check, Reconstruction Test, completion condition 6). Created: [[Resonant Link Labels]] (concept, sprout). |
| SPONT4 | claude.ai Schema/RDP session | 2026-03-28 | Lossy Compression with Intent Alignment + Generative Compression | worthy | | done | Spontaneous deposit. New entries: [[Lossy Compression with Intent Alignment]] (concept, growing), [[Generative Compression]] (concept, sprout). Lost branches logged to Palace To-Do (RDP applied to all ceremonies, chain distillation as stage-transition model, connects-to redemption). Weave flags logged to Palace To-Do (label enrichment pass, new entries into hub nodes, connects-to description update). |
| SPONT5 | claude.ai Enchantment session | 2026-03-31 | First live Palace Enchantment — Stage 1 + Stage 2 complete | worthy | | done | Spontaneous deposit. Forward vectors hand-written for 5 hub entries (Kuramoto Coupling, Four Pillars, Lateral Access, Cooperation Yields Agency, Hilaritas Generator); Loudon edited all five toward behavioral framing. Two enchantments run: Hilaritas Generator and Lateral Access. New entry: [[Tristitia Generator]] (concept, seed, emerged-from Hilaritas Generator). Updated: [[Hilaritas Generator]] (Learning Architect design primitives, 4 new links), [[Lateral Access]] (Taxonomy of Filters, Open Questions section, Confucianism link), [[Palace Enchantment]] (From First Practice findings: synthesis block gap + fix, coordinator/agent voice distinction, forward vector character), [[SUBSTRATE]] (current state), [[Palace To-Do]] (Enchantment next steps). Key finding: synthesis trigger must explicitly name ## SYNTHESIS — STANDING as required artifact. [[Songlines]] seeded in Palace To-Do. |
| D-SESSION-6 | Claude Code session 2026-04-01 | 2026-04-01 | Stage 6 palace enchantment — BBS integration, agency_profile, map injection, active moderator | done | 1 new entry (Scale-Stratified Identity); body additions to Threshold Conatus (threshold's own conatus, open question resolved) and Action Potential Oscillator (refractory-as-threshold-conatus); graffiti note in Cooperation Yields Agency in Loudon's voice |
| D001 | Cowork session 2026-04-03 | 2026-04-03 | Songlines | done | Created [[Songlines]] (concept, growing). Updated [[Lateral Access]] and [[Oblique Portrait]] with wikilinks and YAML frontmatter links. |
| D002 | claude.ai session 2026-04-08 | 2026-04-08 | Gemma 4 local coordination guide | done | Created [[Gemma 4 — Local Coordination Guide]] (source, sprout). Artifact: Artifacts/Tools/GEMMA4_GUIDE.md. Links: connects-to [[BBS Blackboard]], connects-to [[Swarm Weave]]. Lost branch: request-level vs. prompt-level tool coupling as philosophical concept — held for when more LLM families are in play. |

| D-CS-001 | Cowork session 2026-04-09 | 2026-04-09 | Crystal Synthesizer / Metaphor Stretch | done | Created The Metaphor Stretch (concept) + What Claim Does Scientific Sonification Make? (question); updated Crystal Synthesizer to fruiting/working with four Decision Points |
| D003 | claude.ai session 2026-04-09 | 2026-04-09 | Generative Audio Devices — VCV Rack Stage 1 | done | New entries: [[Generative Audio Devices]] (project, sprout, active), [[Registry Pattern]] (concept, seed). Lost branches: RNBO codebox~ Stage 2, validation script, registry auto-generation from source. Productive tension: Registry Pattern contradicts [[Lateral Access]] — closed vocabulary vs. rhizomatic openness. |
| D-2026-04-21b | Cowork session | 2026-04-21 | Wavetable synthesis torus topology + material physics library | done | Created 'Synthesis Space as Torus' (T²/T³ geometry, torus knots, monodromy, cross-domain resonances) and 'The Curve Is the Material' (Wavetable B as parametric function library); updated IWS frontmatter |
| D004 | Cowork session 2026-04-26 | 2026-04-26 | 2D Torus Wavetable Synthesizer + DSP in Looping Dimensions | done | Loudon-authored deposit (paste-in). New entries: [[2D Torus Wavetable Synthesizer]] (project, growing, active — Projects/) and [[DSP in Looping Dimensions]] (concept, sprout — palace root). Concrete instrument paired with the unifying principle it incarnates. Ghost node: [[Loudon Live]] referenced in 2D Torus body but not yet a file. Weave flags: missing connections to [[Inharmonic Wavetable Synthesis]], [[Wavetable Space as Torus]], [[Wavetable Synthesis -- Research & Higher-Dimensional Design]], [[Boundary-Crossing Instruments]], [[Categorizing Inharmonicity]] — held for future Weave rather than silently added. Lost branches in 2D Torus: wavetable-as-filter, 4D hyperprism, The Crystal (DDSP), persistence homology, T^N convolution kernels. |
| D005 | Cowork session 2026-04-28 | 2026-04-28 | Loudon Live + Autodidact Polymaths | done | Resolved the 2026-04-27 persistent forward-ghost. New entries: [[Loudon Live]] (hub, growing — palace root) and [[Autodidact Polymaths]] (concept, sprout — palace root). Loudon Live deposited as `type: hub` (chosen over project/practice given ≥5 incoming typed links and umbrella-organizing role); launch state pre-launch, platform YouTube long-form. Autodidact Polymaths deposited as the audience entry, pillar `practice` only, kept deliberately thin pending lineage reconstruction. Actual reference count surfaced was ~15 substantive entries (vs the 4–5 tracked in Palace To-Do). Body wikilinks added across [[Toolkit — Synthesizers]], [[Toolkit — Audio Plugins]], [[Hyperdimensional Prism]], `Projects/curriculum-map.md`, [[Crystal Synthesizer — Staging]], [[Shepard Tone Synthesizer — Staging]]. Open questions carried: Loudon Live § Open Questions (cadence, launch threshold, monetization, signature instrument, live moments, palace-as-content); Autodidact Polymaths § Open Questions (origin, lineage, distinctness in 2026, audience scale, self-recognition vs recruitment). Reciprocal YAML enrichment intentionally deferred to next Weave for bidirectional adjacency surfacing. Note: the [[Loudon Live]] entry was further reframed on 2026-05-09 (D-2026-05-09 below) as `type: project` with launch-kit framing; the hub-shape architectural content from this deposit is preserved in the merged entry. |
| D006 | Cowork session 2026-04-28 | 2026-04-28 | 4 Pillars Framework - The Founding Conversation | done | Resolved a persistent (4-cycle) forward-ghost that was structurally peculiar — the entry already existed in `Artifacts/` with intact YAML but a wiped body and a Loudon-to-Claude HTML comment instructing recovery from git ("this was way too much, pull this out of the past git as well"). Recovered the original 309-line content from git commit `a17bbd7` (pre-trim). Created tight palace-root breakthrough entry [[4 Pillars Framework - The Founding Conversation]] (~110 lines, type: breakthrough, mature). Saved long-form to [Artifacts/4 Pillars Framework/4 Pillars Framework - Founding Conversation (Long Form).md](../Artifacts/4%20Pillars%20Framework/4%20Pillars%20Framework%20-%20Founding%20Conversation%20%28Long%20Form%29.md) with type changed to `source` since the breakthrough role now belongs to the palace-root entry. Deleted the wiped Artifacts file (its YAML preserved into the new entry; body content lives in the long-form). Fixed dead link `[[AI Partnership Philosophy]]` → `[[Palace AI Partnership Philosophy]]` in the long-form. The 6 entries that cite via `emerged-from` ([[Leverage Points Framework]], [[Quality Manifesto]], [[Palace AI Partnership Philosophy]], plus 3 in `Artifacts/`) now resolve unambiguously to the palace-root entry. Lost branch: [[4 Pillars Weekly Structure]] forward-ghosted in YAML — deferred to its own session per Loudon. Ceremony observation: this is a new shape of deposit — *recovery deposit* — where prior content needs to be unwiped from git history. Worth surfacing if it recurs. |
| D-2026-05-09 | Cowork session 2026-05-09 | 2026-05-09 | Loudon Live — public-teaching-space framing + launch kit + asset variant system | done | Reframes the [[Loudon Live]] entry from `type: hub` (per D005) to `type: project, status: active` with launch-kit framing — the merge-resolution preserved both the hub-shape architectural content (curriculum spine, RTM series, visual identity, reputation architecture, audience) and the project-shape declarative front matter (editorial posture, format, lost branches). Bundle created at `Loudon Live/` containing operational kit: launch kit (channel copy, OBS card text, going-live posts) and asset plan (variant system spec — locked grammar / fresh skin separation, palette library, generator catalog, SVG-first Python toolchain). Editorial posture explicitly named: subject is sound and music, tools are the medium; honesty is a light touch, not a confession; the channel is itself a teaching artifact. Lost branches: schedule cadence undecided, confessional framing rejected, tools-first identity rejected. Asset work proceeds against the maker-architecture pattern (sdxl-maker / musicgen-maker etc.). |
| D-SHOP-01 | Claude Code session 2026-05-09 | 2026-05-09 | Shop architecture deposit + population | done | Pattern + provisional-bet question coupled. New concept entries: [[The Shop]] (concept, sprout) and [[Diversity of Thought in Many-Agent Systems]] (question, seed). New operational entries in `Shop/`: [[Maker]] (alive) + 14 Specialists — 6 alive (Kokoro, Midjourney, ComfyUI, Manim CE, Whisper, ffmpeg) and 8 stubs (Remotion, p5.js, Mermaid, Matplotlib, Stable Audio Open, RNBO codebox~ smith, VCV Patch Generator, Tone.js). Ghost nodes still present on intent: [[Shop/HTML React Artifact]] (planned), [[Shop/Plotly]] (planned), [[Shop/Graphviz]] (planned), [[Shop/Three.js]] (planned). Schema flag: `type: specialist` and `type: maker` are de facto in use but not yet in SCHEMA.md's canonical type list — Schema Ceremony candidate, executed in SC02 below. Lost branch: plural-Designer architecture, deferred pending what Diversity of Thought produces. |
| SC02 | Claude Code session 2026-05-09 | 2026-05-09 | Schema Ceremony v1.7 — formalize `specialist` and `maker` entry types | done | Schema Ceremony, additive (MINOR). CLAUDE.md v1.6 → v1.7, SCHEMA.md doc v1.2 → v1.3. Version step on top of SC01 (Bundle pattern + Handoff Ceremony, 2026-05-05) which had taken v1.6 / v1.2 — version-collision resolved during merge to main. Updated: SCHEMA.md (Decision Tree, Type Definitions, Required Fields with `pillars`/`stage` exception, Type-Specific Required Fields, new §3.2 with full rationale), CLAUDE.md (version, last_schema_ceremony, Key Vocabulary entry types line), ROSETTA.md (§2 type table, §6 file architecture map), Substrate Skill.md (YAML frontmatter template — type list brought to canonical and `pillars`/`stage` exception noted). Validates 14 specialist + 1 maker entries previously schema-violating. Rationale: the Shop pattern introduces operational anatomy (Charter, Tiers, Job Contract, accumulated Gotchas) that practice/meta/source could not carry without distortion. `pillars` made optional for both types because tool-citizens auto-tagging `[tools]` would dilute the pillar signal. `stage` made optional because Specialists/Makers use `status: alive \| stub` rather than the seed→fruiting lifecycle. Forward vector: watch for a second sub-system (Studio/Library/Lab) emerging with its own Maker — that exercises the type's plurality. |
| D-CW-01 | Claude Code session 2026-05-26 | 2026-05-26 | Closing Well — punchlist + dual-channel + verify discipline | done | New practice entry [[Closing Well]] codifies three sub-practices from Loudon's catches during Kuramoto Round 1: the Closing Punchlist (specific cognitive scaffold for cross-instance handoff), Dual-Channel Comprehensibility (educational artifacts must work audio-alone AND video-alone — accessibility, not preference), and Verify To Your Best Ability (use every available tool to check before declaring done, and say so explicitly when verification isn't possible). Latent variable: what it means to declare work done across a boundary. Pillars: practice + philosophy + tools. Stage: sprout. 7 typed links with 7 labels — every link carries semantic compression. Lost branches: Midjourney↔ComfyUI Comparison (Discord-only, Round 1 deliverable still open), LaTeX install for MathTex HUDs, the order-parameter dial reading prose for Kuramoto Coupling.md, the always-extract-frames Specialist gotcha. Weave flags: Maker's Delivery section needs expansion; Shop should reference Closing Well as a standard; every Specialist's Self-Check should produce a punchlist; Substrate Skill could grow a default punchlist scaffold. Productive tension with Maker's single-step Delivery — extension, not contradiction. Memory entry feedback_educational_videos_voiceover.md predates the deposit and is now generalized into the palace entry's Dual-Channel section. |
| D-2026-05-27-OE | Cowork session 2026-05-27 | 2026-05-27 | Oblique Enrichment + Semantic Webcam | done | New meta entry [[Oblique Enrichment]] (sprout) — a deck-shaped variant of [[Enrichment]] that provokes entries with paired image+sound; facet=card rides the existing Enrichment server (iframe HTML artifacts + response box = per-card comment/iterate), with both `couples-with` and `contradicts` to Enrichment (breadth-vs-lingering tension). New project [[Semantic Webcam]] (growing, Projects/) — camera rendered as typeset language; spawned by the flock facet; the multiply-blend links to [[Convolution]]. 13 + 6 typed links, all labeled. Artifacts filed: `Oblique Enrichment/radio-play-deck.html`; `Projects/Semantic Webcam/{01-live-stigmergy-cam,02-word-flock-cam,03-character-multiply-cam}.html`. Routed stigmergy refs to [[BBS Blackboard]] (canonical) rather than a dangling `[[STIGMERGY]]`; findability fix queued in Palace To-Do (alias-vs-rename, deferred). Lost branches: eyes/fingers word-flock cam, "poetry that fits the blobs" (grammar/markov), language-as-a-tonal-medium concept. Weave flags: [[Enrichment]], [[Brian Eno]], [[BBS Blackboard]], [[Synthesis ↔ Emergence]]; converging agent-surface threads held apart on purpose. |
| D-CW-02 | cowork-stewardship-2026-05-27 | 2026-05-27 | Drift and Consolidation (log↔entry drift; stewardship-marker disclosure) | done | 1 new entry (Drift and Consolidation) + Under-Active-Stewardship footers on GSL & GWL |
| BATCH01 | Palace-Deposit-Bundle — Mar–Jun staged batch | 2026-06-05 | DSP + synthesis dissolution cluster (Tier 1+2) + Spinoza person entry | done | 10 new entries planted from `_ops/Deposit Staging/` after schema reconciliation against v1.8. Concept entries to palace root: [[Dispersion Table]] (sprout, working — wavetable-as-lossless-waveguide identity; phase-response-curves-as-tabulated-warp), [[Exponential Decay Curvature]] (sprout — curvature knob's closed form; envelope-becomes-oscillator at audio rate; soft-knee mirror), [[Linear Predictive Coding]] (sprout — prediction=compression=intelligence; source-filter mirrors waveguide scattering; Spinoza Philosopher Visit), [[Chebyshev is Fourier]] (sprout, `who_leads: human` — exact identity under x=cosθ; surface-design via wallpaper-group selection rules), [[Volterra Kernels and the Torus]] (sprout — torus-and-kernel as one N-D field; diagonal=Hammerstein; opens non-diagonal slice trajectories), [[Phase Reduction]] (sprout — Floquet↔Kuramoto bridge via PRC-as-Floquet-object; recursive close at the population scale), [[Rank-N Lattice Analysis]] (sprout — synth-as-oscilloscope; harmonicity as rank-1; pitch/modal/cepstral as one operation), [[Infeasible DSP Now Shippable]] (seed, hypothesis — strategy/taste-as-moat claim; control-surface-design as the hardest problem). Project entry to `Projects/`: [[Bayesian Granular Synthesizer]] (sprout, active — MIDI as Bayesian evidence sculpting a pitch posterior; pitch-distance gates grain length; V7/V8 confirmed-good). Partner person entry to palace root: [[Spinoza]] (seed, `type: person`, 1632–1677) — the house philosopher behind [[Spinoza Conatus]], finally named as a Philosopher Visit anchor and routed to its first confirmed visit at LPC. Schema corrections applied throughout: pillars music→creation / technology→tools; confidence high→working (or hypothesis for the seed); status dropped from concept entries; v1.8 link ontology with labels on most links. Ghost-vs-existing reconciliation against the bundle: Waveguide Synthesis→[[Waveguide Synthesizer]], Hilaritas→[[Hilaritas Generator]], Rhythm-and-Pitch-as-One-Continuum→[[Frequency-Time Duality]], Soft-Knee→[[Compressor Design]], Neurological Synthesizer→[[Neural Granular Synthesis]]+[[Action Potential Oscillator]]. Genuine ghosts left as forward tension: [[Wavetable Oscillator]], [[Karplus-Strong]] (sketch lives in Waveguide Synthesizer bundle), [[Phase Response Curve]], [[Hopf Fibration]], [[Dynamic Convolution]], [[Hammerstein Model]], [[Bayesian Inference]], [[Eulers Number]], [[The Right Representation Reveals the Symmetry]]. Through-line worth naming: nearly every entry is a *dissolution* — wavetable↔waveguide, envelope↔waveform, synthesis↔processing, Chebyshev↔Fourier, Floquet↔Kuramoto, analysis↔synthesis — the single-substance instinct made operational. Weave flags: backlinks into [[Floquet Theory]] and [[Kuramoto Coupling]] pointing at Phase Reduction; mirror/same-object link audit across the dissolution cluster; "Dissolutions" or "One Object, Two Doorways" hub candidacy. Lost branches per entry preserved in their respective `## Lost Branches` sections. Artifact gap: V8 Bayesian-granular code, Dispersion Table FFT-phase-warp pseudocode, curvature-control teaching HTML, eight granular renders — all referenced in source chats and not retrieved into this deposit; flagged in each entry's `## Artifact` section for future pull. Ceremony shape note: batch-style deposit (9+1 entries from a backlog README) rather than the canonical one-source-per-session rhythm — first instance of this shape in the archive. |
| D-2026-06-08-RL | Cowork session 2026-06-08 | 2026-06-08 | Review Layer — reviewable-by-default standard + drop-in kit | done | New concept entry [[Review Layer]] (seed; philosophy/tools/practice): the goal of any artifact is alignment, alignment takes iteration, so first versions ship *reviewable* — a section-level feedback surface built in, flipped off to ship clean. Method is invented to fit the format (not one widget); granularity rule = one moment per natural unit (a card/slide/section, never one per slider). Positive proof = the oblique harvest (`_ops/Harvest Ceremony/Archive/oblique-harvest-final.html`: per-card response + structured JSON export); Murmuration demoted to the over-granular *lesson*. Forward direction: post responses straight to [[STIGMERGY]] to kill copy-paste; couples-with [[Enrichment]] (same respond-in-context loop, opposite direction). Planted at three call surfaces: design system (scoped Hard Rule in `_ops/loudon-live/design-system/SKILL.md` + drop-in kit `ui_kits/review-layer/` — `review-layer.js` + README, per-item JSON + markdown export), the [[Maker]] (inherited house standard + interactive-Specialist Self-Check line), and the entry itself as canonical. CLAUDE.md Tier-0 floor deliberately left untouched (Loudon's call). 9 typed links, all labeled. Lost branches: non-DOM review methods (timecoded video, image markup overlay, sound-against-playhead) left to invent; the STIGMERGY review-post channel unbuilt. Weave flags → WEAVE board: STIGMERGY (build a review-response post channel), Enrichment (adopt the shared respond+post surface). Commit handed off to Mac-side via cowork-git (canon → not lock-moved from Cowork). |
| D-2026-06-09-BLS | Cowork session 2026-06-09 | 2026-06-09 | Bundle-Local Stewardship — content/machinery split production plan | done | New project entry Bundle-Local Stewardship — Production Plan (Palace development/, type project, status pending, stage seed) — a Claude-Code build contract that applies one standing principle, **the Machinery/Content Split** (shared engine → _ops, per-entry content → the entry's bundle), to the whole stewardship system. Names six stranding points where entry-owned content sits in `_ops/agents/permanent/[slug]/` (pending/resolved decisions, the duplicated stewardship vector/stage block, history.jsonl reasoning, staging plans, GSL's loose HANDOFF/STAGE-A-LESSONS, graffiti harvested to Palace To-Do). Design is CQRS, not relocation: board stays the append-only event log, a new `[Entry] — plan.md` bundle file becomes the materialized read-model (Plan/Open Decisions/Resolved/Done + pointer to forward_vector, never a copy), `_ops` keeps slim runtime (iteration/cursor/health). Five phases (ratify §8 `plan` type → additive orchestrator change reading vector/stage from frontmatter + materializing plan → backfill 19 live stewards → clean strays + normalize REGISTRY dir paths → verify). Cross-surface handoff written to the entry bundle for the Mac-side Claude Code session. 9 typed links, 7 labeled. Forward tension: plan.md↔Staging reconciliation; whether history.jsonl wants a bundle mirror; whether the Machinery/Content Split graduates to its own concept entry. Commit handed to Mac-side via cowork-git (touches running heartbeat + canon → not lock-moved from Cowork). |
| D-2026-06-09-LENS | Cowork session 2026-06-09 | 2026-06-09 | The Lens — page-on-page lensing concept + bundle | done | New concept entry [[The Lens]] (palace root, growing, working) — one operation with three faces: the *operation* (render passage A through the apparatus of page B), the *instrument* (a draggable glass = the [[STIGMERGY Entry-Agent Window — Integration Plan v0.1\|companion window]] in a new mode), and the *genre* (pages written to be worn). Generalizes [[Philosopher Visits the Entry]] from "a philosopher over an entry" to "any page over any passage," grounded in the palace's own founding claims: [[Pages as Agents]] (the glass invokes page-B-as-agent), [[Mixture of Experts]] (lensing = routing to a node), [[Meaning and the Link]] (a lensing *is* the edge rendered as content, so the lens is a link-discovery instrument — a handheld [[Weave Ceremony\|Weave]]). Names the real design crux: the mechanism is trivial, the **signal** is everything (N² pairs, most noise; lean on the typed-link graph to suggest sparking pairs; show how hard a pairing lit up; turn a spark into a committed edge). Two cardinal lens-sets with *different payoffs*: philosophers *challenge a claim* → link/Visit; the **Four Pillars** *complete a projection* (the [[Hyperdimensional Prism]] operationalized; every entry has four latent faces) → deposit candidate. Lens has an **arrow** (A-through-B ≠ B-through-A) and a **fidelity test** (move the glass → reading must change specific to the text). **Schema stance held open per Loudon's let-categories-emerge preference**: lens is a *role/mode* a page opts into (`lens_profile` or `member-of [[Lenses]]`), NOT a `type: lens` Schema Ceremony yet. Bundle `The Lens/` created with two interactive mockups (any-page-through-any-page with spark meter + propose-link; philosopher refraction/confrontation toggle). `[[STIGMERGY Philosophical Lenses]]` positioned as the philosophy-first special case to be re-scoped to *the* lens surface. Ghost left as invitation: `[[Lenses]]` registry hub (the home lens entries opt into). Reciprocal wiring NOT yet applied (Philosopher Visits `exemplifies` The Lens; back-links on Pages as Agents / FOUR PILLARS / Hyperdimensional Prism) — held for Loudon's nod, touches mature canon. Commit handed to Mac-side (canon → not lock-moved from Cowork). |
| D-2026-06-09-LATEX | Claude Code session 2026-06-09 | 2026-06-09 | LaTeX Specialist + Manim dvisvgm gotcha refresh + tex-env shim | done | Born from a capability question ("is LaTeX installed; does the Shop know how to use it?") that became a verified-on-host investigation. New specialist entry [[LaTeX]] (Shop/, type specialist, status **stub**, medium other, tool TeX Live) — typeset documents/papers/beamer/`tikz`/`pgfplots`/standalone equation cards; boundary drawn vs [[Shop/Manim CE\|Manim CE]] (math that *moves*), [[Shop/Matplotlib\|Matplotlib]] (charts from *data*), [[Shop/Mermaid\|Mermaid]] (quick diagrams) — and one TeX install serves all three (Matplotlib's `text.usetex` + Manim's `MathTex` both ride it). Findings (verified on full Homebrew TeX Live 2026 / Manim CE 0.20.1 / Py 3.13.2): the **PDF path** (`pdflatex`/`xelatex`/`latexmk`→PDF) runs clean; the **SVG path** (`latex`→`dvi`→`dvisvgm`→`svg`, used by Manim `MathTex` and standalone cutouts) **fails bare** and is fixed by `TEXPSHEADERS="$(kpsewhich -var-value=TEXMFDIST)/dvips/{base,config}"` — confirmed end-to-end via a real `MathTex` render. Root cause pinned: brew's standalone `dvisvgm` formula owns the bin and TeX Live's own `dvisvgm` is a *symlink to it*, so its kpathsea never sees the TeX Live tree regardless of completeness — correcting the older "basictex is incomplete" framing (`TEXMFCNF` alone does not fix it). Refreshed [[Shop/Manim CE]] with a dated 2026-06-09 gotcha (re-pinned cause + version-agnostic fix), bumped `last_tested`/`last_gotcha`, removed a stray duplicate "Last run: never." line. Killed the tribal-knowledge problem with a shared shim `Artifacts/Shop/tex-env.sh` (wrapper + `--print` eval modes, resolves `TEXPSHEADERS` via `kpsewhich`) that both Specialists source — smoke-tested + real-SVG-proofed. Wired LaTeX into the [[Maker]] (directs link, Selection Heuristic, Roster, Stub 1→2, fallback table) and `Artifacts/Shop/host-capability.json` (capability check confirmed reachable on mac). **No Schema Ceremony** — adding an entry instance is routine; `specialist`/`maker` types canonical since SC02/v1.7. Committed Mac-side, staging only this deposit's files (working tree carries unrelated in-flight changes). Forward: first real document brief promotes stub→alive; open sub-questions — move `tex-env.sh` to `_ops` if a non-Shop caller appears, and whether a shared `palace.sty` preamble (Loudon Live fontspec stack + six-skin palette) is worth building. |
| D-2026-06-09-TRICKSTER | Cowork session 2026-06-09 (Trickster Commit deposit) | 2026-06-09 | Trickster Commit — trusted-hand write-anywhere mode | done | New concept entry [[Trickster Commit]] (palace root, seed; tools/philosophy/practice) — the Companion's armed-write path worn as its trusted-operator inverse: write **anywhere** (canon allow-list bypassed), in **one gesture** (no propose→approve round trip), branded `Palace-Author: trickster` + `verify: unverified` so the weird stratum stays *harvestable* rather than noise. Recklessness held safe by structure, not hope: git's honest 3-way revert is the net (reach-everywhere is licensed because undo is symmetric with commit), branding-not-hiding keeps the diff truthful, and path-safety (`..`/NUL/repo-escape) is never dropped — only canon *care* is. exemplifies [[Trickster]] (`Neighborhood: EVERYWHERE` finally reaching the keyboard); connects-to [[STIGMERGY]] (rides-the-armed-write-path) and [[Closing Well]] (honest-about-unverified). Posture, not pipeline: a mode flag on `armedWriteEntry`/`previewEdit`, not a forked lane — same propose→commit→undo machinery, inverted. Phase-1 build (mode flag splitting path-safety from canon, one-gesture commit lane, gesture surface in the Companion window, `Palace-Author: trickster` tint in the LOG deck) handed to this Mac-side Claude Code session via [[Trickster Commit — build baton]]. Rejected this session: trickster-non-canon-only (he reaches everything), a separate trickster pipeline, synchronous confirm-before-land, deferred digest grounding (phase 2), a new `trickster` commit-kind / Schema change (`author` is a free-form trailer; kind stays `edit`). Open questions: grounding in-commit vs. deferred to a trickster digest; whether canon wants a louder confirm even for the trickster; what the digest composts; whether the branded-stratum pattern licenses other reckless capabilities (trickster delete/merge). |

| D-2026-06-09-EQRENDER | Cowork session 2026-06-09 | 2026-06-09 | STIGMERGY equation rendering build plan | done | New project entry STIGMERGY — Equation Rendering Build Plan (Palace development/, type project, stage seed, energy high) — a Claude-Code build contract teaching STIGMERGY to render math on the phosphor grid instead of dumping raw TeX. Three moves: an inline pass (prettify $…$ to Unicode sub/superscript spans, ships first), a LaTeX-subset grid engine (Option B: \sum/\frac/^/_/\cdot laid on the character cell with a stacked Sigma + CSS fraction rules), and the dual-channel framed block (Option C: symbolic line then where-key then worded line) as the display format. KaTeX (Option A) rejected: proportional math fonts cannot be gridded + breaks the no-CDN house rule. Worded line is an author-time WRITING STANDARD, never render-time; past equations corrected by a Weave task. Bundle `STIGMERGY — Equation Rendering Build Plan/` holds the four-option mockup (`equation-rendering-proposals-mockup.html`). 4 typed links, all labeled. Weave flag: the worded-form writing standard wants its own palace entry (currently only memory `feedback_equations_with_words`). Committed from Cowork via cowork-git lock-safe committer (small non-canon dev entry). |
| D-2026-06-13-BLUELINE | Cowork session 2026-06-13 (committed fb62370; backfilled Mac-side) | 2026-06-13 | BLUELINE seed deposit — project + Shop/Blender Specialist + toyxyz recipe + two anchor concepts | done | **Backfill of a Cowork deposit committed out-of-ceremony.** Commit `fb62370` ("deposit(blueline): seed BLUELINE project + Shop/Blender + two anchor concepts") planted the BLUELINE pre-deposit set from Cowork at Loudon's explicit instruction, *bypassing the formal Deposit Ceremony* — the commit body (`Palace-Verify: unverified`, `Palace-Author: claude`) flagged "Deposit Archive entry to be backfilled Mac-side," recorded here. 7 files / 590 insertions: **[[BLUELINE]]** (`Projects/BLUELINE/`, type project, status active, stage seed, pillars tools/creation/philosophy — the opinionated music-video animatic engine, novelty is *point of view and coupling not components*, fixed-tempo, comic↔cinema two-register transduction, animal motion explicitly OUT of scope as of 2026-06-13); **[[Shop/Blender]]** (Shop/, type specialist, status **stub**, medium 3d, tool blender 5.0 — the 3D blocking room + offline render bench, the hand-editable surface, differentiated from [[Shop/Three.js]] as offline/hand-authored vs real-time/browser); **[[Shop/Blender/toyxyz-conditioning-recipe]]** (recipe inside Shop/Blender, status untested — posed scene → OpenPose/DWPose/depth/normal/canny/lineart for multi-ControlNet; the one hard rule = one preprocessor + one ControlNet per channel); **[[Blocked, Not Prompted]]** (concept, seed, working — author composition as geometry so the model *fills* rather than *chooses*; defeats the front-on prompt-only default); **[[The Flow Field is the Spine]]** (concept, seed, hypothesis — one authored vector field at three resolutions of reality: drawn speed-lines / dense motion conditioning / simulated medium; "the arrow becomes the wind"; the #1 risk and novel bet); plus the two bundle meta docs **[[BLUELINE — Deposit Map]]** (the show-before-writing proposal + build-vs-adopt map) and **[[BLUELINE — Claude Code Job]]** (the multi-session technical spike brief). Decisions locked in the deposit: toyxyz is a *recipe inside Shop/Blender* not its own Specialist; bundle docs before skills; staging-AI bake-off goes in betting Blender-MCP #1 / SceneCraft #2; pre-deposit set frozen, all other candidate tools wait on the Claude Code job so only proven tools earn entries (frugality bar). Redundancy check at backfill (Mac-side): no existing Blender/OpenPose/conditioning entry in the palace → Shop/Blender is net-new; the ghost reference [[ControlNet Workflow Mastery]] (cited by Shop/ComfyUI + Shop/Maker, never deposited) is the natural home for the Session-1 multi-ControlNet findings — flagged, not yet written. Verification status carried from the commit: `unverified` (the toyxyz recipe + Shop/Blender stub get *verified* and promoted to alive in Session 1 of the Claude Code job, this same Mac-side run). |
| D-2026-06-14-BLUELINE-SPIKE | Claude Code session 2026-06-14 (Mac-side, GPU + RunPod) | 2026-06-14 | BLUELINE technical spike — all five Production-Plan tracks exercised end to end | done | **The full BLUELINE technical spike** across one long Mac-side session — not a canon deposit but a large body of project-proof work recorded here for continuity (git is the archive; 18 commits, `git log --grep BLUELINE`). **Track I (GPU substrate) ✅ both tiers:** board→serverless FLUX→retrieved (palace-flux, `serverless_runner.py`) + a RunPod pod (4090) driving **FLUX-ControlNet via the WAF-hardened transport** (browser-UA + curl), geometric pose enforced; `pod_runner.py` (board-driven merge), `runpod` host class wired into `host-capability.json`, network volume `blueline-models` stood up. **Track III (Clock) ✅ LIVE-VALIDATED:** the (bar,beat)→frame determinism recipe + OSC→WS relay + browser clock, driven by **Loudon's real Max-for-Live device from live Ableton** (120@24 → 12 frames/beat, beats on whole frames, ~5 ms σ); the M4L device build spec + two device fixes (reuse-LiveAPI perf, and the `qmetro`/validity-guard "no-valid-object" fix — *unverified* in Max, pending Loudon's test). **Track IV (Bench) ✅:** pose library + camera-grammar solvers + true-OTS environment (IV-D alley) → board records. **Track V (motion coherence, the #1 risk) ✅ workable:** seed-locking defeats the stitched-stills flicker (quantified color_corr 0.94 vs 0.17); first rung, full stack named. **Track II:** the assess ruler (`consistency_ruler.py`) ✅ + an 8-image consistent character dataset ✅ (shared-seed method, ruler-validated embed 0.93); the LoRA **training kit ready but not run** (needs an SSH-enabled pod — `track-II-lora/TRAIN.md`). Also: Blender-MCP staging path **live-tested** (resolved Deposit-Map #4 — MCP interactive primary / SceneCraft headless backend / Three.js preview, one shared spec). RunPod account left clean (0 pods; volume kept ~$2/mo; endpoint parked); ~$0.65 spent of a $20/day budget. Two open threads + full state captured in `[[BLUELINE — baton]]` for clean-context pickup. No canon entries created or changed; no Schema Ceremony; the Shop/Blender promotion + host-capability edits were the only canon touches (separately committed). |
| D-2026-06-15-WTSCAN | Cowork session 2026-05-31 → 2026-06-15 (backfilled with v1.10/v1.11 schema corrections) | 2026-06-15 | Wavetable Scanner — paired-Specialist Sketch + concept entry + Shop entry edits + schema-corrective backfill | done | **Bundled deposit covering an out-of-ceremony Cowork build (2026-05-31) and its schema-corrective backfill (2026-06-15) after v1.10/v1.11 schema ceremonies moved the canon underneath the earlier writes.** Two new entries to palace root: [[Wavetable Scanner]] (type project, status active, stage sprout, pillars creation/tools — single-cycle wavetable morph laboratory; CRT skin; the first paired-Specialist Sketch the palace has shipped — [[Shop/Web Audio Worklet]] for DSP + [[Shop/Three.js]] for the 3D visualizer reading the same `Float32Array` the worklet reads, so picture and sound cannot drift; the [[Waveguide Synthesizer]] pattern in miniature) and [[1D Wavetable Scanning]] (type concept, stage sprout, pillars creation/tools — names the patterns the build surfaced: that morphing is a 2D linear interpolation per output sample, that frame ordering carries the axis's meaning, that exploration-class instruments default to drone-by-default while voice-class default to gated, and the single-source-of-truth pattern; emerged-from the Wavetable Scanner artifact). Five new dated gotchas appended to [[Shop/Web Audio Worklet]] (decodeAudioData neutering its input; AudioContext-can-decode-while-suspended; transferable-vs-retain decision rule generalising the Murmuration gotcha; wavetable-position-morph-is-2D-linear-interpolation; drone-by-default-for-scanners) + `last_tested`/`last_gotcha` rolled to 2026-05-31. [[Shop/Three.js]] **promoted stub → alive** on this first dated job (ahead of the still-anticipated [[Projects/Waveguide Synthesizer|Waveguide Synthesizer]] commission); three anticipated traps confirmed (r128 ceiling, `needsUpdate` silent no-op, version-pinned CDN URLs) + one new (`LineBasicMaterial.linewidth` 1px ceiling on most platforms); Wavetable Scanner recipe added; frontmatter `status` + links updated (pairs-with [[Shop/Web Audio Worklet]], exemplified-by [[Wavetable Scanner]]). [[Shop/Maker]] roster updated: Alive 16 → 17 with Three.js promotion line; Stub 2 → 1; `Wavetable Scanner` recipe row. Bundle migrated from `Artifacts/Wavetable Scanner/` → root-level `Wavetable Scanner/` per SCHEMA §8 (owned files live in the entry's bundle, sibling to the .md); HTML's CSS link path corrected `../../` → `../`; gotchas markdown migrated into the bundle as [[Wavetable Scanner — gotchas]] (trying `gotchas` as a new §8 bundle-file type per the "new types may be tried freely" invitation). Schema-corrective backfill (2026-06-15): both new entries gained `title`, `pillars` (out of `tags`), `born: 2026-05` (replacing the wrongly-named `adopted: 2026-05-31`), and `forward_vector` frontmatter sentences; Wavetable Scanner gained `status: active` (project-type required field) and dropped its default-declaring `tier_vocabulary` per the Maker's "only rename when the renaming is doing real work" rule. Lint-doc-drift run clean afterward: 0 errors, 32 warnings (one less than before — my pre-backfill `outputs/wavetable_scanner_gotchas.md` warning is gone; the remaining 32 all pre-date this deposit). Inherited non-compliance not corrected: existing Shop entries (Web Audio Worklet, Three.js, Maker) use `adopted:` rather than the canonical `born:` field — flagged for a wider Shop-section cleanup, not touched by this deposit. **Pending mac-side cleanup**: the now-empty `Artifacts/Wavetable Scanner/` directory cannot be deleted from Cowork (sandbox can rename but not delete — see CLAUDE.md §Committing from Cowork); needs the cowork-git lock-safe committer or a mac-side `rmdir`. Verification status `unverified` for the in-browser audible/visual confirmation per the Web Audio Worklet entry's Sketch-tier bar; node --check clean on both the worklet body and the main app script (with `${FRAME_LEN}` substituted). |

| D-2026-06-14-RUNPOD | Cowork session 2026-06-14 | 2026-06-14 | RunPod GPU Backend — Shop's GPU compute layer + FLUX/ControlNet pipeline | done | New concept entry [[RunPod GPU Backend]] (Shop/, type concept, stage growing, pillars tools/creation) — the compute substrate beneath the Specialists. Core content: the **serverless-vs-pod routing decision** (scale-to-zero/spiky vs continuous-bill/full-ComfyUI+ControlNet); a hard-won **operational playbook** (match worker-image VRAM to GPU pool — FLUX full weights OOM 24GB, fp8 fits; `allowedCudaVersions` ≥12.6 to dodge old-driver hosts; broaden GPU pool to beat throttling; cold-start patience / don't recycle mid-pull; flashboot off for big images; container disk must hold the image; **verify one frame before batching**); the **FLUX + ControlNet OpenPose pose-locked restyle workflow** (the technique kept as a section — *breakthrough candidate for a future Weave*) with the honest **img2img failure** that motivated it (pixel-diffs showed era prompts expressed ~nothing because pose-lock and bold-restyle fight inside one denoise number); **model routing** SDXL (~3.5B, stylized, weak text/hands) vs FLUX (~12B, T5, strong text/hands), Maker routes per brief; and **tooling** (Palace Studio local app + lifecycle-managing launcher, `pod-comfyui-client.py` with the proxy-WAF browser-UA + curl-multipart gotchas, verbatim passthrough). Bundle `Shop/RunPod GPU Backend/`: backend-design.md, serverless-client.py, pod-comfyui-client.py, walkcycle-pose-generator.py, walkcycle-movie-batch.py, flux-controlnet-openpose.workflow.json. One concrete edit: added the **`runpod` host class** to `Artifacts/Shop/host-capability.json`. Reframe banked: a pod buys **capability, not freedom** — RunPod adds no request filter to either surface; the variable is the checkpoint, not the infrastructure. Typed links (6, labeled): enables [[The Shop]], couples-with [[Maker]], connects-to [[ComfyUI]] / [[FLUX (Hugging Face)]] / [[Quality Manifesto]] / [[Capability-first prototyping]]. Untested horizons (Loudon-requested): temporal coherence (AnimateDiff / WAN / LTX) to fix the walk-cycle flicker+leg-swaps, identity-lock across frames (IPAdapter/PuLID), network volume so models persist across pods, full-precision FLUX (HF-gated), ControlNet depth/canny/tile, the Image-to-3D Specialist now that a real GPU exists, RIFE interpolation, LoRAs/fine-tunes, Studio-app pod-mode. Artifacts (not bundled, live under `RunPod Images/`): SDXL gallery (38 imgs) + FLUX gallery (60 imgs) + reports + two era-morph movies. Weave flags: elevate the ControlNet technique to its own [[breakthrough]] + wire to the Image-to-3D commission; reciprocal backlinks from [[The Shop]]/[[Maker]]/[[ComfyUI]] not yet applied (held for Weave). Commit handed to Mac-side (canon → not lock-moved from Cowork). |
| D-2026-06-16-LORA-EVAL | Claude Code session 2026-06-15→16 (Mac-side, RunPod GPU + STIGMERGY) | 2026-06-16 | LoRA Trainer Specialist + the Review-Layer→STIGMERGY poster realized (Loudon Eval) | done | Two keepers from a long BLUELINE Track-II session. **(1) [[Review Layer]] updated** (stage seed→growing): its central forward vector — *a review surface that posts straight to [[STIGMERGY]], killing copy-paste* — is now **realized**. Built `_ops/loudon-eval/` (a Node `eval-server.mjs` + reusable `rate.html` harness): serves a per-task `rating_manifest.json` + images same-origin, and on Send **forwards** the structured responses server-to-server to the app's sanctioned write path `POST /api/persistent`, wrapped as a `human_eval` BROADCAST from `TRICKSTER` on board `FLAGS` — the STIGMERGY app's own strict validator gates it (no re-implementation; gotcha: the `health` stub needs the four Path-1 fields with `iteration ≥ 1`). Added as the entry's third proof (after the oblique harvest + Murmuration); forward vector rewritten (prize won → generalize the poster beyond the rating-sheet shape; [[Enrichment]] to adopt it). First use: graded BLUELINE's `r4ng3r` LoRAs — Loudon's in-context rating both **confirmed the metrics** and surfaced a distinction they'd blurred (a description locks the *brief*/costume, not the *person*/identity) — the first real evidence for the entry's open question that reviewing-in-context changes feedback character. **(2) New [[Shop/LoRA Trainer]]** (Shop/, type specialist, status alive, medium image, tools ai-toolkit/kohya, torch 2.6+cu124) — train a character/subject LoRA on RunPod first-try. Operational anatomy: two Tiers (FLUX/ai-toolkit Piece · SDXL/kohya Study), Job Contract, the first-try Recipe, and the **six gotchas** that cost the session (flash-attn red-herring→it's torch; diffusers-main needs torch≥2.5→bump 2.6; torchaudio version-match; HF_TRANSFER-without-pkg fake-tokenizer error; macOS scp `-l` rate-limit; render-after-train OOM). Grade method = DINO+ArcFace+CLIP-T (*never* ResNet18, which inverted the v1 verdict), plus the **DreamBooth-control move** — run a known-good subject to separate pipeline-fault from dataset-fault (corgi DINO **0.776** vs r4ng3r **0.317**, same recipe → pipeline sound, dataset is the lever). 4 typed links (labeled): connects-to [[Shop/RunPod GPU Backend]] (rides-the-substrate) / [[BLUELINE — Production Plan]] (track-II-trainer); member-of [[The Shop]] (roster-member); exemplifies [[Capability-first prototyping]] (prove-then-optimize). **Verification:** eval loop round-trip-proven (a rating posted + read back off the board); the LoRA Trainer recipe proven (two DreamBooth-control LoRAs trained first-try after hardening); the r4ng3r LoRAs themselves **failed** — an honest negative (net-negative vs no-LoRA, both metric- and human-confirmed). Project-proof artifacts (M0 previz + clip-scan, the four LoRAs + grade renders, the train/grade scripts in `Projects/BLUELINE/proofs/track-II-lora/` + `proofs/m0-previz/`) are git-archived, not canon. **Weave flags:** [[Enrichment]] should adopt the same poster; the Review-Layer Hard Rule in [[Loudon Live Design System]]/[[Maker]] can now say "posts to STIGMERGY" not "exports to clipboard"; generalize the `human_eval` poster payload beyond the rating-sheet shape. Two test `human_eval` messages remain on the append-only board as benign scaffolding. Closing steps (weave-flag board posts + git commit) pending Loudon's go. |
| D-2026-06-16-SHOP-COMPLIANCE | Claude Code session 2026-06-16 (Mac-side) | 2026-06-16 | Shop schema-compliance migration (Phase 1) — the wider Shop cleanup flagged by D-2026-06-15-WTSCAN | done | **Maintenance pass, not a canon deposit — recorded for continuity (git is the archive).** Brought the whole Shop into SCHEMA compliance: the v1.6 ceremony defined `specialist`/`maker` but never migrated the pre-existing entries, leaving 22 (21 specialists + [[Shop/Maker]]) on the legacy shape. For all 22: added `title`, replaced non-schema `adopted:` with `born:` (YYYY-MM), authored a conatus `forward_vector`, and retyped every frontmatter link against §4 (each gained a `type:` + `[[wikilink]]` target; the bare-`label`-only form is gone) — every Specialist now declares `member-of [[The Shop]]` (the roster's single source of truth) plus `connects-to [[Maker]]` (directed-by); redundant `wraps→(external)` and folder-path `tested-by` pseudo-links dropped. [[Shop/Blender]] illegal `medium: 3d`→`image` (+ `submedium` dropped); [[Shop/Three.js]] stray `submedium` dropped. **Dead-path repair:** `Artifacts/` was deprecated 2026-06-16 but ~45 files still pointed at `Artifacts/Shop/…`; repaired to bundle paths (Shop-root toolchain → `Shop/Maker/…`, specialist refs → `Shop/<name>/…`), and fixed the resulting `PALACE = parents[4]→parents[3]` depth bug in render.py/pipeline.py. **Bundle frontmatter (§8):** 8 frontmatter-less bundle `.md` files given minimal YAML; two bundle files carrying an invalid entry-`type` (`recipe`, `meta`) demoted to bundle form. **One functional fix:** `host-capability-check.js` `HOST_CLASSES` was missing the `runpod` class the manifest already had → test now 10/10 green. SCHEMA §3.2 gained a dated note correcting the v1.6 "all validate after" claim (factual correction, not a Schema Ceremony). Verified: custom frontmatter linter 0 violations across 25 entries + 27 bundle files; `lint-doc-drift.py` 0 errors. **Phase 2 (done, same session):** (1) roster single-source-of-truth — `Shop/Maker/build-roster.mjs` renders the roster (22 Specialists: 19 alive · 2 stub · 1 deprecated) into a generated `<!-- ROSTER -->` block in [[The Shop]] § Roster from each Specialist's `member-of` link; the Maker's hand-listed roster became a pointer (drift-immunity round-trip verified: flip a status → table tracks → restore). (2) `Shop/Maker/run-tests.mjs` — the Shop's first real test harness: host-capability-gated, runs 6 cheap real smokes (p5.js/D3.js/Observable Plot/Tone.js via web-smoke, ffmpeg encode+decode, Matplotlib render) → 6 pass / 0 fail, the rest honestly reported as skip-unreachable or reachable-but-not-wired; `--write-back` rolled `last_tested: 2026-06-16` onto the 6 passers. The harness surfaced + fixed a stale `host-capability.json` (missing Blender/LoRA Trainer/Web Audio Worklet). Verified: frontmatter linter 0 violations, lint-doc-drift 0 errors, host-capability 10/10. |
| D-2026-06-17-PIPELINE | Claude Code session 2026-06-17 (Mac-side) | 2026-06-17 | BLUELINE production pipeline — adopt the established animation craft, author only the seams | done | New concept entry [[Adopt the Craft, Author the Seam]] (palace root, type concept, stage growing, pillars tools/philosophy/creation). The keeper from a long BLUELINE conversation: we caught ourselves *reinventing film-craft* (the Loomis head sphere, the box of the torso, shot grammar, Disney's Staging principle, McCloud's panel transitions, all re-named under our own words) → the discipline is the opposite: **adopt the established pipeline wholesale** (anime backbone · comics skin · animated-feature connective tissue · music-video clock) and **author only the seams** — the points where a loose-reading human is replaced by an exact-input machine. BLUELINE has exactly two: **(1) board→layout (2D→3D)** — a *handoff* solved by design: one board record rendered at two fidelities (comic cheap · Blender grey-box exact), tune *params not pixels*, depth round-trips → no double-tuning; **(2) layout→render (staging→conditioning)** — the *frontier* = [[Blocked, Not Prompted]] (blocking → exact OpenPose/ControlNet keypoints, which no human pipeline had because its renderer read loosely). One staging vocabulary (facing · eyeline · L/R laterality · the shoulder–shoulder–pelvis torso frame · shot size · camera grammar) does **three jobs at once**: the board-record schema, the AI's conditioning keypoints (seam 2), and the lossless 2D→3D transfer format (seam 1). Generalizes to any AI-in-a-craft (music, code, illustration): find where a collaborator who *interpreted* is replaced by one who *requires exactness* — that point is the only new work. **Productive tension flagged:** contradicts the make-first/[[FOUR PILLARS]] autodidact instinct → resolution: build the seam from scratch (friction teaches), adopt the craft (friction already paid decades ago). 5 typed links (labeled): connects-to [[BLUELINE]] (worked-example) / [[Blocked, Not Prompted]] (the-render-seam) / [[Shop/Blender]] (the-2D-3D-seam); contradicts [[FOUR PILLARS]] (make-first-vs-adopt); mirrors [[Simondon]] (concretization — the seam is where integration concentrates). Lost branches: the parked cited deep-research run (rate-limited; resume for citations); M2 motion comic; Track II face-forward LoRA redo; the 3D-assisted-boarding lane for camera-heavy shots. Weave flags: [[Blocked, Not Prompted]] should gain an `exemplifies` → [[Adopt the Craft, Author the Seam]] link; BLUELINE's "novelty is coupling, not components" forward vector should cite this entry. Same session: merged `feature/blueline`→`main` (61b439d, the M1 comic-register animatic + staging vocabulary — git-archived, not canon) and re-founded BLUELINE on the pipeline (new `Projects/BLUELINE/BLUELINE — Production Pipeline.md`). Open question carried: is there a **third** seam — where AI output feeds *back* into human stages (a render that informs a re-board)? |
| D-2026-06-19-RADIOPLAY | Claude Code session 2026-06-19 (Mac-side; STIGMERGY weave explainer video) | 2026-06-19 | Radio Play craft entry + the audio-first / VO-as-clock method | done | New `practice` entry [[Radio Play]] (palace root, stage sprout, pillars creation/tools/practice) — the Shop's audio-drama craft: unseen multi-voice characters carrying the full meaning in sound (voices, pacing, music, effects), **audio-complete by design**, with visuals/interactivity as *enrichments* layered on the audio spine. The headline reframe is Loudon's, arrived at across three turns: the *radio play* is the craft to master; *visuals* are one enrichment, produced via **VO-as-clock** (render audio first → measure per-segment durations → visuals fill & sync to the spine, never the reverse) — a method that converged from the machine side (can't sync picture to speech it can't hear, so fix audio first) onto Loudon's own audio-first picture-editing practice (*"how I go about editing picture generally"*); that convergence is the deposit-worthy part. 6 typed links (labeled): emerged-from [[The Shop]] (forged-in); connects-to [[Maker]] (dispatched-by) / [[Kokoro]] (casts) / [[Stable Audio Open]] (scores) / [[ffmpeg]] (assembles) / [[FOUR PILLARS]] (sound-leads). Small in-scope updates: [[Modes of Collaboration]] gained a catalogue bullet (*The Debrief Reel* — a visuals-enriched Radio Play made to explain just-finished work for one's own comprehension; the reflecting-surface application, links [[Hilaritas Generator]]) + a frontmatter link to [[Radio Play]]; [[Shop/Maker]] gained a Recipe (*2026-06-19 — The Unsung Path*, the Shop's first radio play, the audio-first/VO-as-clock pipeline, escalation beyond *Narrated Beats*). First instance: the *Unsung Path* explainer (4 character voices, music/SFX/laughs, 5 Manim phosphor-terminal scenes, ~2.5 min, −15.9 LUFS), assets in `_ops/scratch/weave-video/`. "Built by meter, not ear" confirmed by Loudon as proven-sufficient — recorded as validated technique, not a caveat. Weave flags (deferred, NOT yet on the board): per-Specialist operational gotchas to land — [[Kokoro]] multi-voice casting; [[ffmpeg]] whole-video assembly template (per-segment adelay-mix + concat); [[Manim CE]] `Seg` duration-scaling base class; [[Stable Audio Open]] comedic SFX/laughs — plus wire [[Radio Play]] to the dual-channel-rule entry if one exists. Lost branches: word-level Whisper sync (skipped for proportional timing); promoting the reel to a published Loudon Live Piece; codifying a reusable Radio Play pipeline template. Commit on `main` with explicit pathspecs; the shared working tree carried pre-existing unrelated changes (Wavetable Scanner, Artifacts/assets deletions, 4 uncommitted board lines), left untouched. |
