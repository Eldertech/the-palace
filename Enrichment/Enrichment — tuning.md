---
title: "Enrichment — tuning"
born: 2026-09-24
links:
  - target: "[[Enrichment]]"
    type: connects-to
    label: tuning-for
forward_vector: "I am Enrichment's record of what each run taught it, numbered, each lesson tied to the spec change it forced, so the ceremony's version has a reason you can read. Append after every run; never prune what a real run taught."
---

# Enrichment — tuning

What each enrichment taught the ceremony, appended after the run. Each entry names what the run showed and the spec change it forced — or says plainly that it changed only the tool. Newest last.

## From the assessment and the first rich face — Kuramoto Coupling, 2026-09-24

1. **Breadth starved every page, and the machine that fed it failed silently.** Five small cards spread across many entries never transformed one. The queue's worker ran a test stub in May, hit a usage limit in June, and had no retry, so responses sat for four months; the critic passed 29 of 30 cards. Forced: **v2.0** — depth on one page with the whole Shop, no queue, no worker, no critic; the rich face replaces the card.
2. **Gathering paid before making did.** Kuramoto's bundle held two narrations and an ambient bed that nothing linked to. Forced: step 1, gather first.
3. **A piece that tests the text beats one that illustrates it.** The Path B piece confirmed the entry's waveform claim; building the stubbornness piece found a condition the text lacked (an anchor needs K_send as well as K_receive → 0). Forced: step 2's preference and step 6, carry findings home.
4. **The door line counted as prose**, so adding it would have drifted the section it sits in. Tool only: the parser skips it.
5. **Interactive pieces that size a canvas from its own panel feed back** — Kuramoto's iframes grew past 9,000 px. Tool only: fixed canvas heights (`_ops/rich-face/README.md`).
6. **An entry's diagram can share class names with the page** — Kuramoto's Mermaid defines `drift`. Tool only: the renderer's own classes are prefixed.
